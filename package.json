{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./test --bail --timeout 30000 --recursive --slow 1000 --exit",
    "debug": "node-dev ./app.js",
    "start": "pm2 start app.js -n dcpool-api --max-memory-restart 500M --no-daemon"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.17.3",
    "eslint-plugin-promise": "^4.1.1",
    "husky": "^2.3.0",
    "lint-staged": "^8.1.7",
    "mocha": "^9.1.2",
    "prettier-eslint-cli": "^4.7.1",
    "supertest": "^6.1.6"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "dependencies": {
    "koa-enterprise": "^1.0.2"
  }
}
